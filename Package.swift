// swift-tools-version:4.0
import PackageDescription

let package = Package(
    name: "Pretty JSON",
    dependencies: [ ],
    targets: [
        .target(
            name: "Pretty JSON",
            dependencies: []),
        .testTarget(
            name: "Pretty JSONTests",
            dependencies: []),
    ]
)
